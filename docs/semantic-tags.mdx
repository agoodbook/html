---
id: semantic-tags
title: 新增元素
---

import DocTable from "../src/components/DocTable";

HTML5引入了许多新的语义标签，用于更清晰地定义文档的结构和内容。以下是一些新增的全部语义标签：
### 语义标签

<DocTable attribute={
  [
    {"title": "标签", 'dataIndex': 'name', render: (text) => <code>{text}</code>},
    {"title": "描述", 'dataIndex': 'description'},
  ]
}/>

